services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vedun_dev
    ports:
      - "5432:5432"
    volumes:
      - vedun_pg:/var/lib/postgresql/data

volumes:
  vedun_pg:
